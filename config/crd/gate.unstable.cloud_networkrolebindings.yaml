---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.21.0
  name: networkrolebindings.gate.unstable.cloud
spec:
  group: gate.unstable.cloud
  names:
    categories:
    - gated
    kind: NetworkRoleBinding
    listKind: NetworkRoleBindingList
    plural: networkrolebindings
    shortNames:
    - netrolebinding
    singular: networkrolebinding
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .spec.roleRef.name
      name: Role
      type: string
    - jsonPath: .status.conditions[?(@.type=="RoleResolved")].status
      name: Resolved
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: NetworkRoleBinding grants a NetworkRole to subjects.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: NetworkRoleBindingSpec grants a role to a set of subjects.
            properties:
              roleRef:
                description: roleRef names the NetworkRole being granted.
                properties:
                  name:
                    description: name of the NetworkRole in the same namespace as
                      this binding.
                    maxLength: 253
                    minLength: 1
                    pattern: ^[a-z0-9]([-a-z0-9.]*[a-z0-9])?$
                    type: string
                required:
                - name
                type: object
              subjects:
                description: subjects the role is granted to.
                items:
                  description: Subject is a principal a role is granted to.
                  properties:
                    kind:
                      default: User
                      description: kind of principal. Only User exists today.
                      enum:
                      - User
                      type: string
                    name:
                      description: |-
                        name of the principal: github:<login>, google:<mail address>,
                        system:authenticated or system:unauthenticated.
                      maxLength: 253
                      minLength: 1
                      pattern: ^(github:[A-Za-z0-9][A-Za-z0-9-]{0,38}|google:[^@\s]+@[^@\s]+\.[^@\s]+|system:(authenticated|unauthenticated))$
                      type: string
                  required:
                  - name
                  type: object
                maxItems: 128
                minItems: 1
                type: array
                x-kubernetes-list-type: atomic
            required:
            - roleRef
            - subjects
            type: object
          status:
            description: |-
              NetworkRoleBindingStatus reports whether the binding currently grants
              anything.
            properties:
              conditions:
                description: |-
                  conditions of the binding. RoleResolved is set to False when
                  spec.roleRef names a NetworkRole that does not exist.
                items:
                  description: Condition contains details for one aspect of the current
                    state of this API Resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        lastTransitionTime is the last time the condition transitioned from one status to another.
                        This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        message is a human readable message indicating details about the transition.
                        This may be an empty string.
                      maxLength: 32768
                      type: string
                    observedGeneration:
                      description: |-
                        observedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      minimum: 0
                      type: integer
                    reason:
                      description: |-
                        reason contains a programmatic identifier indicating the reason for the condition's last transition.
                        Producers of specific condition types may define expected values and meanings for this field,
                        and whether the values are considered a guaranteed API.
                        The value should be a CamelCase string.
                        This field may not be empty.
                      maxLength: 1024
                      minLength: 1
                      pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                      type: string
                    status:
                      description: status of the condition, one of True, False, Unknown.
                      enum:
                      - "True"
                      - "False"
                      - Unknown
                      type: string
                    type:
                      description: type of condition in CamelCase or in foo.example.com/CamelCase.
                      maxLength: 316
                      pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                type: array
                x-kubernetes-list-map-keys:
                - type
                x-kubernetes-list-type: map
              observedGeneration:
                description: |-
                  observedGeneration is the generation of the spec this status was
                  computed from.
                format: int64
                type: integer
            type: object
        required:
        - spec
        type: object
    served: true
    storage: true
    subresources:
      status: {}
